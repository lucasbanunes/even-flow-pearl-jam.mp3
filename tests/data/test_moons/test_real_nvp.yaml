dataset:
  batch_size: 32
model:
  features: 2
  transforms: 4
  hidden_features: [64, 64]
  activation: relu
  early_stopping:
    monitor: val_loss
    mode: min
  checkpoint:
    monitor: val_loss
    mode: min
  max_epochs: 3
